import 'package:flutter/material.dart';

class RemoveTransactionConfirmationWidget extends StatelessWidget {
  const RemoveTransactionConfirmationWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return AlertDialog(
      title: Text("Confirm the "),
    )
  }
}
